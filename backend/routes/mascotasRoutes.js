// // backend/routes/mascotasRoutes.js

// const express = require('express');
// const router = express.Router();
// const mascotasController = require('../controllers/mascotasController');
// // const authMiddleware = require('../middleware/authMiddleware');

// // Ruta para crear una mascota
// router.post('/', authMiddleware.verificarToken, mascotasController.crearMascota);

// // Ruta para obtener todas las mascotas
// router.get('/', authMiddleware.verificarToken, mascotasController.obtenerMascotas);

// // Ruta para modificar una mascota por ID
// router.put('/:id', authMiddleware.verificarToken, mascotasController.modificarMascota);

// // Ruta para eliminar una mascota por ID
// router.delete('/:id', authMiddleware.verificarToken, mascotasController.eliminarMascota);

// module.exports = router;
